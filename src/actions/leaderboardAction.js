import axios from 'axios';

import { HOST_URL } from './types';
import {
    PAGINATION_CNT,
    INCREASE,
} from './types';

const onGetRankingData = (from, cnt) => (dispatch) => {

};


export default {
    onGetRankingData
}